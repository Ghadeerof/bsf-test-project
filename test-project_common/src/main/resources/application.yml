server:
  port: 8087

spring:
  application:
    name: test-service
  datasource:
    url: jdbc:mysql://localhost:3306/bsf
    username: root
    password:
  jpa:
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: update
  springdoc:
    swagger-ui:
      path: /swagger-ui.html